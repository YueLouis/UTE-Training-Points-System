## 📌 Note (FE + BE Smoke Test)

- Mục tiêu: test luồng **Frontend ↔ Backend ↔ API ↔ Database**
- FE: Android (MVVM + Retrofit)
- BE: Spring Boot (đã deploy Railway)
- API: gọi được danh sách category / event
- DB: MySQL (Railway)

👉 Hiện tại dùng để **verify kết nối end-to-end**, chưa phải bản hoàn chỉnh chức năng.  
👉 Các bạn clone về, cấu hình lại `BASE_URL` là chạy test được ngay.
